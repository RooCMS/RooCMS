<?xml version="1.0"?>
<ruleset name="RooCMS PHP Mess Detector Rules"
         xmlns="http://pmd.sf.net/ruleset/1.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://pmd.sf.net/ruleset/1.0.0
                             http://pmd.sf.net/ruleset_xml_schema.xsd"
         xsi:noNamespaceSchemaLocation="http://pmd.sf.net/ruleset_xml_schema.xsd">
    <description>RooCMS PHP Mess Detector configuration for PHP 8.1+ pure PHP project</description>

    <!-- Rules for code -->
    <rule ref="rulesets/codesize.xml">
        <exclude name="ExcessiveMethodLength"/>
        <exclude name="ExcessiveClassLength"/>
        <exclude name="CyclomaticComplexity"/>
        <exclude name="NPathComplexity"/>
    </rule>

    <!-- Rules for unused code -->
    <rule ref="rulesets/unusedcode.xml">
        <exclude name="UnusedFormalParameter"/>
        <exclude name="UnusedPrivateMethod"/>
        <exclude name="UnusedLocalVariable"/>
    </rule>

    <!-- Rules for naming -->
    <rule ref="rulesets/naming.xml">
        <exclude name="ShortVariable"/>
        <exclude name="LongVariable"/>
        <exclude name="ShortMethodName"/>
    </rule>

    <!-- Rules for design -->
    <rule ref="rulesets/design.xml">
        <exclude name="ExitExpression"/>
        <exclude name="EvalExpression"/>
        <exclude name="GotoStatement"/>
        <exclude name="NumberOfChildren"/>
    </rule>

    <!-- Rules for controversial code -->
    <rule ref="rulesets/controversial.xml">
        <exclude name="Superglobals"/>
        <exclude name="CamelCaseClassName"/>
        <exclude name="CamelCasePropertyName"/>
        <exclude name="CamelCaseMethodName"/>
        <exclude name="CamelCaseParameterName"/>
        <exclude name="CamelCaseVariableName"/>
    </rule>

    <!-- Exclude files -->
    <exclude-pattern>*/vendor/*</exclude-pattern>
    <exclude-pattern>*/node_modules/*</exclude-pattern>
    <exclude-pattern>*/storage/logs/*</exclude-pattern>
    <exclude-pattern>*/upload/*</exclude-pattern>
    <exclude-pattern>*/storage/assets/*</exclude-pattern>
    <exclude-pattern>*.log</exclude-pattern>

    <!-- Exclude markdown and documentation files -->
    <exclude-pattern>*.md</exclude-pattern>
    <exclude-pattern>README.md</exclude-pattern>
    <exclude-pattern>RELEASE.md</exclude-pattern>
    <exclude-pattern>LICENSE.md</exclude-pattern>
    <exclude-pattern>*/README.md</exclude-pattern>
    <exclude-pattern>*/database/README.md</exclude-pattern>

    <!-- Exclude configuration files -->
    <exclude-pattern>*.yml</exclude-pattern>
    <exclude-pattern>*.yaml</exclude-pattern>
    <exclude-pattern>*.xml</exclude-pattern>
    <exclude-pattern>*.json</exclude-pattern>

    <!-- Analyzed files -->
    <files>
        <include-pattern>roocms/**/*.php</include-pattern>
        <include-pattern>api/**/*.php</include-pattern>
        <include-pattern>storage/**/*.php</include-pattern>
    </files>

    <!-- Minimum thresholds -->
    <properties>
        <property name="minimum" value="10"/>
        <property name="maximum" value="100"/>
    </properties>
</ruleset>
